ANTHROPIC_API_KEY=
PORT=3000

# the server only binds to localhost by default, on purpose. uncomment this to
# bind somewhere reachable from outside your machine, but only do that if you've
# actually thought about rate limiting and API cost exposure, since this endpoint
# can call a paid API
# HOST=0.0.0.0
